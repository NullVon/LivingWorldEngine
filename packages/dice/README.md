# Optional Dice package

Deferred for this milestone. No Dice implementation or dependency is installed.

A future Shell may use an isolated Dice package inside an Action resolver and return ordinary Event/effect specifications. The six-pillar Core must continue to run and test without importing that package. The legacy `lwe_dice.js` has not been inspected or ported.
